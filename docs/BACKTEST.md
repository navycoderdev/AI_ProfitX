# BACKTEST Guide

Set `TRADING_MODE=BACKTEST`. Use stored raw history and the Phase 4 replay engine. Signals see only completed history; fills occur on the next candle open with configured spread, commission, slippage, margin and conservative stop/target handling.

Inspect reports for costs, drawdown, sample size and regime segmentation. Historical P&L is research evidence, not a profitability guarantee.
