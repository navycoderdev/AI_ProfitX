# SHADOW Guide

Set `TRADING_MODE=SHADOW`. The active candidate processes current market snapshots and emits logged hypothetical proposals. `ShadowEvaluationEngine` has no order execution dependency. Collect enough shadow observations and validate behavior before an approved model can be considered for controlled LIVE promotion.
